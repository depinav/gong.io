# Gong.io Coding Assessment

## To run the app, from the root of the directory:
* run `pnpm install`
* run `pnpm run dev`
* navigate to the url shown in the terminal output

## Tests
* run `pnpm run tests`